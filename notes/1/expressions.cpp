#include <iostream>

int main() {
  // 1. Literal expression
  42;

  // 2. Arithmetic expression
  int result = 5 + 2 * 2;

  // 3. Relational/Boolean expression
  bool isGreater = (10 > 5);

  // 4. Assignment Expression 
  int x{};
  x = 20;

  // 5. Logical Expression
  bool logicalCheck = (x > 10 && result == 0);
  

  return 0;
}